import time

def demo_without_stream():
    print("\n--- 1. 非流式 (stream=False) ---")
    print("正在请求服务器 (假设服务器需要 3 秒生成一篇作文)...")
    # 模拟服务器思考和生成的耗时
    time.sleep(3) 
    full_text = "春天来了，万物复苏。小草从土里探出头来，花儿也笑了。"
    # 此时用户盯着空白屏幕发呆了 3 秒，然后一瞬间看到了所有字
    print(f"🤖 AI: {full_text}")

def demo_with_stream():
    print("\n--- 2. 流式 (stream=True) ---")
    print("正在请求服务器...")
    # 模拟服务器边想边吐字
    full_text = "春天来了，万物复苏。小草从土里探出头来，花儿也笑了。"
    
    print("🤖 AI: ", end="", flush=True)
    for char in full_text:
        # 模拟每个字生成的微小延迟
        time.sleep(0.1) 
        # 立刻打印这个字，不换行
        print(char, end="", flush=True)
    print() # 最后换行

if __name__ == "__main__":
    demo_without_stream()
    demo_with_stream()
